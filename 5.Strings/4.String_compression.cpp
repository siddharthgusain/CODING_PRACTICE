#include <iostream>
#include <string>

std::string compression1(std::string str)
{
    std::string final_string = "";
    final_string.push_back(str.at(0));
    
    return "";
}

int main()
{
    std::string str;
    std::cin >> str;
    compression1(str);

    return 0;
}